# Ignore Node.js dependency directories
node_modules/
.env
.DS_Store
*.zip

# Ignore logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore package-lock.json files if not needed (though it's often good to commit them)
# package-lock.json
# yarn.lock 